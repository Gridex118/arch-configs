custom {

    binds {
        # Mouse binds
        bindm = ALT, mouse:272, movewindow
        bindm = ALT, mouse:273, resizewindow
        bind = ALT, mouse_down, workspace, e-1
        bind = ALT, mouse_up, workspace, e+1

        # Application binds
        bind = SUPER, Q, killactive,
        bind = SUPER, RETURN, exec, $term
        bind = SUPER_SHIFT, RETURN, exec, $browser
        bind = SUPER_CTRL, RETURN, exec, ~/.config/hypr/scripts/fzathura.sh
        bind = SUPER_CTRL_SHIFT, RETURN, exec, ~/.config/hypr/scripts/fzathura.sh --menu
        bind = , XF86AudioPlay, exec, ~/.config/hypr/scripts/music.sh

        # Other dispatchers
        bind = ALT_SHIFT, T, exec, ~/.config/hypr/scripts/key_delay.sh
        bind = SUPER_SHIFT, N, exec, ~/.config/hypr/scripts/nettoggle.sh
        bind = ALT_SHIFT, M, exec, ~/.config/hypr/scripts/touchpad.sh
        bind = SUPER, Print, exec, notify-send "New Screenshot"
        bind = SUPER, Print, exec, GRIM_DEFAULT_DIR=~/Screenshots grim
        bind = SUPER_SHIFT, Tab, workspace, previous
        bind = SUPER, P, pseudo,
        bind = SUPER, M, fullscreen, 1
        bind = SUPER, SPACE, togglefloating,
        bind = , F9, exec, swaync-client -t

        # Fullscreen Submap
        bind = SUPER, A, exec, ~/.config/hypr/scripts/fullscreen.sh    # Only change submap if something's gone fullscreen
        submap = fullscr
        bind = SUPER, Tab, changegroupactive,
        binde = SUPER, A, fullscreen,
        binde = SUPER, A, submap, reset
        submap = reset

        # Groups Submap
        bind = SUPER_SHIFT, SPACE, togglegroup,
        bind = SUPER, Tab, changegroupactive,
        # bind = SUPER, G, exec, notify-send "Submap Groups"
        bind = SUPER, G, submap, groups
        submap = groups
        binde = , Q, killactive,
        binde = , T, changegroupactive,
        binde = , U, moveoutofgroup,
        binde = , G, togglegroup,
        binde = SHIFT, H, moveintogroup, l
        binde = SHIFT, J, moveintogroup, d
        binde = SHIFT, K, moveintogroup, u
        binde = SHIFT, L, moveintogroup, r
        binde = SUPER, H, movewindow, l
        binde = SUPER, J, movewindow, d
        binde = SUPER, K, movewindow, u
        binde = SUPER, L, movewindow, r
        binde = , H, movefocus, l
        binde = , J, movefocus, d
        binde = , K, movefocus, u
        binde = , L, movefocus, r
        # binde = , escape, exec, notify-send "Global Map"
        binde = , catchall, submap, reset
        submap = reset

        bind = SUPER_SHIFT, D, exec, $launcher
        # Application Submap
        bind = SUPER, D, submap, appli
        submap = appli
        binde = , P, exec, ~/.config/hypr/scripts/appli_launch.sh "firefox --private-window"
        binde = SHIFT, P, exec, ~/.config/hypr/scripts/appli_launch.sh "firefox --private-window" --no-reset-submap
        binde = , E, exec, ~/.config/hypr/scripts/appli_launch.sh $editor
        binde = SHIFT, E, exec, ~/.config/hypr/scripts/appli_launch.sh $editor --no-reset-submap
        binde = , F, exec, ~/.config/hypr/scripts/appli_launch.sh nemo
        binde = SHIFT, F, exec, ~/.config/hypr/scripts/appli_launch.sh nemo --no-reset-submap
        binde = , S, exec, ~/.config/hypr/scripts/appli_launch.sh obs
        binde = SHIFT, S, exec, ~/.config/hypr/scripts/appli_launch.sh obs --no-reset-submap
        binde = , 1, exec, ~/.config/hypr/scripts/appli_launch.sh pcsx2
        binde = SHIFT, 1, exec, ~/.config/hypr/scripts/appli_launch.sh pcsx2 --no-reset-submap
        binde = , escape, submap, reset
        submap = reset

        # Relaod config
        bind = SUPER_SHIFT, R, exec, hyprctl reload
        bind = SUPER_SHIFT, R, exec, notify-send "Config Reloaded"

        # Logout Hibernate, logout, lock
        bind = ALT_SHIFT_CTRL, escape, exec, systemctl hibernate
        bind = ALT_SHIFT, escape, exec, hyprctl dispatch exit
        bind = ALT, L, exec, loginctl lock-session


        # Move window with SUPER + Shift + [hjkl]
        bind = SUPER_SHIFT, l, movewindow, r
        bind = SUPER_SHIFT, h, movewindow, l
        bind = SUPER_SHIFT, k, movewindow, u
        bind = SUPER_SHIFT, j, movewindow, d

        # Move window focus with SUPER + [hjkl]
        bind = SUPER, l, movefocus, r
        bind = SUPER, h, movefocus, l
        bind = SUPER, k, movefocus, u
        bind = SUPER, j, movefocus, d

        # Move To a workspaces
        bind = SUPER, 1, exec, hyprctl dispatch workspace 1
        bind = SUPER, 2, exec, hyprctl dispatch workspace 2
        bind = SUPER, 3, exec, hyprctl dispatch workspace 3
        bind = SUPER, 4, exec, hyprctl dispatch workspace 4
        bind = SUPER, 5, exec, hyprctl dispatch workspace 5
        bind = SUPER, 6, exec, hyprctl dispatch workspace 6
        bind = SUPER, 7, exec, hyprctl dispatch workspace 7
        bind = SUPER, 8, exec, hyprctl dispatch workspace 8
        bind = SUPER, 9, exec, hyprctl dispatch workspace 9

        # Move windows between workspaces
        bind = SUPER_SHIFT, 1, movetoworkspace, 1
        bind = SUPER_SHIFT, 2, movetoworkspace, 2
        bind = SUPER_SHIFT, 3, movetoworkspace, 3
        bind = SUPER_SHIFT, 4, movetoworkspace, 4
        bind = SUPER_SHIFT, 5, movetoworkspace, 5
        bind = SUPER_SHIFT, 6, movetoworkspace, 6
        bind = SUPER_SHIFT, 7, movetoworkspace, 7
        bind = SUPER_SHIFT, 8, movetoworkspace, 8
        bind = SUPER_SHIFT, 9, movetoworkspace, 9

        # Use this to get thw XF86 bind (FN + Fx combination) for your keyboard
        # xev | grep -A2 --line-buffered '^KeyRelease' | sed -n '/keycode /s/^.*keycode \([0-9]*\).* (.*, \(.*\)).*$/\1 \2/p'
    }

}
